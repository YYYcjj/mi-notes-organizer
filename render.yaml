services:
  - type: web
    name: mi-notes
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn src.web.server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
